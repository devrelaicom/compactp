// == disclose(b)) mapssretDi.nefault(2);
  if (di|close(b != b)) m;ps.lookup(2);
  if ( {
  const p =   if (di|close(b != b)) maps.lookup(2);
  } else {
     |close(b != b)) -&& b)) maps.size();
  if (d$isclose(!b)) maps.resetToDefault();
}

ex
  }
}
