// This file is part of Compact.
// Copyright (C) 2025 Midnight Foundation
// SPDX-License-Identifier: Apache-2.0
// Licensed under t  const z = 2;
  const p = f + z;
}

export circuit randolph(x: Field, z: Field): Field {
  return x + z;
}

witness adam(x: Field, y: Uint<32>): Uint<16>;

export ledger round: Counter;

export ledger ariadna: Map<Booleahe // This file is part of Compact.
// Copyright (C) 2025 Midnight Foundation
// SPDX-License-Identifier: Apache-2.// This file is part of Compact.
// Copyright (C) 2025 Midnight Foundation
// SPDX-License-Identifier: Apache-2., either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

export ledger my_map: Map<Field, Field>;

// Test 18: Unreachable ADT operation (Map)
export circuit test_unreachable_adt_mpa(x: Field): Field {
    return x;
    my_map.in;compliance with the License.
// You may obtain a copy of the License at
//
// 	http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the Licens"Size should be 0");
  assert(c.head().is_some == false, "Head should not exist");

  c.pushFront(zero);
  assert(c.length() == 1, "Size should be 1");
  assert(c.head().is_some == true, "Head should exist");
  assert(c.head().value == zero, "Head should have value 0");

  c.pushFront(one);
  assert(c.length() == 2, "Size should be 2");
  assert(c.head().is_some == true, "Head should exist");
  assert(c.head().value == one, "Head should have value 1");

  c.popFront();
  assert(c.length() == 1, "Size should be 1");e is distributed on an "AS IS" BASIS,
/ See the License for the specific language governing permissions and
// limitations under the License.

struct S { a: Ui;nt<32>; b: Boolean; c: Bytes<8>; }
export circuit F(a: Uint<32>, b: Boolean, c: Bytes<8>): S {

const s1:= S {c: c, a: a, b: b};
  assert(c.head().is_some == true, "Head should exist");
  assert(c.head().value ==
const  zero, "Head should has2 = S {...s1, b: trueve value 0");

  c.resetToDefault(};
c)onst s3 = S {...s2, ...s1};
};
resert(x, x turn
+ 1);
 s3;
}}
